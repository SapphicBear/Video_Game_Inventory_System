<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head.ejs"); %>
</head>
<body>
    <header>
        <%- include("./partials/header.ejs"); %>
    </header>
        <main>
            <ul>
                <% games.map((game, i) => { %>
                    <li> 
                        <div class="gameContainer">
                            <h2>Game <%= i + 1 %></h2>
                            <p><%= game.game_name %></p>
                            <p><%= game.studio_name %></p>
                            <p><%= game.genre %></p>
                            <p><%= game.release_year %></p>
                            <p><%= game.console %></p>
                            <p><%= game.price %></p>
                            <p><%= game.in_stock %></p>
                        </div>
                    </li>
                <% }); %>
            </ul>
        </main>
        <aside class="sidebar">
            <%- include("./partials/sidebar.ejs", { genres: genres, consoles: consoles }) %>
        </aside>
    <footer>
        <%- include("./partials/footer.ejs", { links: links }); %>
    </footer>
</body>
</html>